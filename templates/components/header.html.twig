<header>
    <div class="nav">
        <a href="{{ path('main_index') }}"><img src={{ asset('img/logo.png') }} alt="logo wonderGeeK"></a>
    </div>
    <ul class="nav justify-content-center">
      <li class="nav-item">
        <a class="nav-link px-4 text-light" href="{{ path('main_index') }}">Accueil</a>
      </li>
      <li class="nav-item">
        <a class="nav-link px-4 text-light" href="{{ path('main_event') }}">Évenements</a>
      </li>
      <li class="nav-item">
        <a class="nav-link px-4 text-light" href="{{ path('main_contact') }}">Contact</a>
      </li>
    </ul>
     {% if app.user %}
            <a href="#">
                <i class="fas fa-user"></i>
                {{ app.user.username }}
            </a>
            <a href="{{ path('user_logout') }}">
                Déconnexion
            </a>
        {% else %}
            <a href="{{ path('user_login') }}">
                Connexion
            </a>
            <a href="{{ path('user_register') }}">
                Inscription
            </a>
        {% endif %}
</header>